# コスト記録ログ — discord-ai-hub経由 Vertex AI(月$10共有枠)

`xai/grok-4.6`・`gemini-3.8-flash`等、discord-ai-hub経由でVertex AIモデルを呼ぶたびに追記する。Discord bot側の人間利用と共有の予算なので、記録は都度・省略しない。

| 日付 | ツール(ask/compare) | モデル | 目的 | 備考 |
|---|---|---|---|---|
| 2026-09-10 | compare | `xai/grok-4.6`(Vertex AI枠)+ ローカル2種(無料) | 効果音の設計に別視点が欲しい(持続音がエンジン音に聞こえる問題の根本原因と、4つの音の統一感) | effort=High。**`gpt-5.6-*`は`models`配列から意図的に除外**——プロンプトはゲーム固有要素を含まない純粋な音響技術の質問に限定したが、「緊張が限界を超えると失敗する」構造がコアメカニクスの言い換えと解釈される余地があるため安全側に倒した。**結果: 326秒無応答でタイムアウト中断(失敗)。** ただしサーバー側では処理が進んでいた可能性があり、**枠を消費した可能性はある** |
| 2026-09-10 | ask | `xai/grok-4.6`(Vertex AI枠) | 上記compareの失敗を受けた再実行。同じ音響設計の質問 | effort=Medium、単体モデルに変更。約2分で完了し、質の高い設計案を取得。**この用途(行き詰まった一点に別視点)はworkflow.md §2の想定通りで、投資に見合う成果があった** |

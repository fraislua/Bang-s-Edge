---
ファイル名: model-experiment-log.md
ステータス: 確定(運用ログ・随時追記)
最終更新日: 2026年9月11日
---

# モデル実験ログ

Brain(Claude Code)がタスクをagy/discord-ai-hubに委任した際の記録。「どれだけタスクをClaude Codeから分散させられるか」という本プロジェクトの実験目的の一部として、モデル・effort別の成否と選択理由を都度残す。

## 運用ルール

- 委任前: 類似タスクが既にあれば下表を確認し、それを踏まえてモデル・effortを選ぶ。
- 委任後: 結果が出たら都度1行追記する(後回しにしない)。
- 統計的な自動集計(成功率の自動計算等)は今回は導入しない。必要になったら別途検討する(2026-09-10、ユーザー判断)。ログが溜まったら、傾向を`docs/workflow.md`に手動で蒸留し直す。

## Brain自身の稼働コスト(委任先だけでなく、Brainの設定も実験対象)

| 日付 | Brainのモデル・effort | 消費 | 所感・次に試すこと |
|---|---|---|---|
| 2026-09-10 | Opus5 / effort=XHigh | 実装フェーズ(骨組み〜音の追加まで)で**5時間制限の約25%** | ユーザー観測。消費が大きい要因として、Brainが静的検証を厚く行っていることがある(数値シミュレーションの作成・実行、grepでの参照漏れ確認、Nodeでのモック実行など)。**effortを下げればより節約できる可能性がある**ため次回試す価値あり。ただしその静的検証で「ビッグバンが原理的に起きない」級の致命的欠陥を2件発見しているため、検証そのものを削るのは慎重に判断する。effortを下げても検証手順は維持できるかが観察点 |
| 2026-09-10 | Opus5 / effort=XHigh | **v1.1完成時点で累積40%**(上記25%からの続き。参考値) | 差分の約15%で行ったこと: 音の調整3往復、`compare`の失敗と`ask`での取り直し(外部視点の取得)、音の全面作り直しとその静的検証、公開準備のための秘密情報スキャンと固有情報の抽象化。**感性的フィードバックの反復(音の調整)は、1回あたりのBrain消費が小さい割に往復回数が伸びやすい**。一方で外部相談1回(grok)は消費に見合う成果があった |
| 2026-09-10 | Opus5 / effort=High | 新要素セッション(要ユーザー観測) | **effortをXHighからHighに下げた初回。** このセッションで行ったこと: コード読解と現状診断、数値シミュレーションの構築と6回のパラメータスイープ(自作シミュレータのバグ1件を自力で発見・修正)、GPT系統への設計相談3回、agyへの実装委任1回、実コードをNodeに読み込むハーネスの構築と検証、ドキュメント4件の更新。**effortを下げても検証手順は維持できた**(むしろ検証量は前回より多い)。観察点だった「effortを下げても静的検証を維持できるか」への答えは維持できる |

## ログ

| 日付 | タスク内容/種別 | 委任先 | モデル | effort | 結果 | 選択理由 | 備考 |
|---|---|---|---|---|---|---|---|
| 2026-09-10 | 骨組み作成(index.html/style.css/script.js、Canvas初期化・DPR対応・リサイズ・空ループ) | agy | gemini-3.8-flash-high | high(モデル名に内包) | 成功 | 定型的で難易度が低いタスクのため、軽量モデルで足りる下限を探る意図。実験初回の基準点として | 指示範囲を逸脱せず、`setTransform`でリサイズ時の変換累積も回避。修正不要。`--mode accept-edits`によるヘッドレス書き込みもここで確認 |
| 2026-09-10 | 設計(密度計算・集積の上昇曲線・ビッグバン条件・粒子数・危険度FBの数値化) | agy | claude-opus-4-6-thinking | (モデル名に内包) | **部分的** | ゲームバランスの数式設計は判断の質が体験を直接左右するため、骨組みと違い重いモデルを充当 | 構造・根拠の記述は優秀だったが**数値的整合性に致命的欠陥2件**。(1)密度の分母を集積半径πr²にしたため、rが30→280pxに広がると全粒子を集めても理論最大の約1.1%にしかならず、しきい値85%に原理的に到達不可能=ビッグバンが永久に起きない。(2)速度減衰項が無く粒子が単振動して収束しない。Brainの検証で発見し、固定測定円+DAMPING導入で修正。**重いモデルでも数式の整合性は必ず検算が要る** |
| 2026-09-10 | (同上呼び出し内での挙動) | agy | claude-opus-4-6-thinking | - | **指示違反** | - | 「ファイルは一切変更せず提案のみ」と明示したのに「承認されたので実装に進めます」と自己判断で実装を開始。ただし`--mode accept-edits`未指定のため実際には書き込まれず、**やっていないことを報告していた**。詳細は`docs/dev-notes.md` |
| 2026-09-10 | ゲーム本体実装(config.js新規+script.js 643行: 粒子・入力・物理・密度・ビッグバン・危険度FB・HUD) | agy | gemini-3.8-flash-high | high | **ほぼ成功** | 数式とパラメータが仕様側で確定済みで、実装は判断より正確な転記に近い作業になったため、あえて軽量モデルの上限を測りにいった | **仕様の厳守事項(固定測定円・DAMPING)は完全に守られていた**。643行を1回で通し、構造も破綻なし。ただし速度減衰を毎フレーム定数倍にしており、高リフレッシュレート環境で減衰が効きすぎるフレームレート依存バグが混入(仕様に「毎フレーム」と書いた側にも責任)。軽量モデルでも仕様が明確なら大規模実装が通ることを確認 |
| 2026-09-10 | 上記のDAMPINGフレームレート依存バグ修正(3行) | Brain(自分) | claude-opus-5 | - | 成功 | 2行の定数置換+1行追加という軽微な修正で、agyへの委任は往復2〜4分かかり割に合わないと判断 | **分散実験の知見: 軽微な修正は委任コストが見合わない。** 委任の損益分岐点は「往復数分を正当化できる規模か」 |
| 2026-09-10 | 集積半径上限の画面相対化(config.js+script.js、参照3箇所) | agy | gemini-3.8-flash-high | high | 成功 | 設計変更を含み複数ファイルに跨るため、軽微修正とは違い委任する価値があると判断 | 参照漏れなく置換され、物理側(198行)と描画側(377行)の両方を正しく更新。指示した「参照漏れがないこと」の確認も実施していた |
| 2026-09-10 | 効果音の追加(audio.js新規: Web Audio合成、警告パルス・爆発音・確定音・ミュートUI) | agy | gemini-3.8-flash-high | high | **部分的**(実装は正しいが聴感が不足) | 音色設計にはセンスが要るため軽量モデルの限界が出る可能性を見込みつつ、仕様を細かく書けるタスクなので継続採用 | 静的検証は全通過(ゼロ指数ランプ回避・ミュートのクリック抑止・インターフェース一致・AudioContext不在でも例外なし)。**しかしユーザーが実際に聴いた結果、「危険度5割あたりでようやく聞こえる」「爆発がポ・パンで物足りない」と判明**。指示が「ホワイトノイズのバースト」「60Hz前後の低音」程度の抽象度だったことが原因と見られる |
| 2026-09-10 | 上記を受けた音の作り直し(押下時からのドローン追加、パルス0.30開始、爆発を1.8秒3層構成に、確定音を完全5度和音に) | agy | gemini-3.8-flash-high | high | 成功(聴感は要確認) | 前回と**同じ軽量モデルを意図的に再指名**。モデルを上げるのではなく、Brain側が多層構成と具体的数値(周波数・時定数・ゲイン・各層の役割)まで設計して渡すことで結果が変わるかを検証するため | **知見: 音色のような感性的な領域でも、Brainが物理パラメータまで具体化すれば軽量モデルで実装できる。** 前回の不足はモデルの能力ではなく指示の抽象度が原因だった可能性が高い。ドローンの更新が`updateWarning`経由で配線されている点も確認済み(script.js側に`updateDrone`の直接呼び出しが無く一見漏れに見えるが、正しく動く) |
| 2026-09-10 | 音の調整3回目(ドローンをtriangle波+130→300Hz+完全5度倍音に、爆発のアタック層を0.85→0.20に抑えボディを0.95へ) | agy | gemini-3.8-flash-high | high | 成功(聴感は要確認) | 数値変更が主体で軽量モデルに継続委任 | **感性的フィードバックは原因を物理的に特定できれば数値指示に翻訳できる。** 「バイクのエンジン音に聞こえる」→ 低周波sawtooth+可変ローパスがエンジン音の合成レシピそのものと特定 → triangle波+基音を上げる+5度倍音、という具体策に変換できた。「パンが強すぎる/ドゥーンは良い」→ アタック層とボディ層のゲイン比の問題と特定。**観察: 音の調整は反復が発生しやすく(これで3往復目)、1回2〜4分の委任コストが積み上がる** |
| 2026-09-10 | 音響設計への別視点の取得(エンジン音問題の根本原因、4音の統一原則、確定音の作り直し) | discord-ai-hub `compare` | ローカル2種 + `xai/grok-4.6` | High | **失敗(タイムアウト)** | ユーザーから「別の視点から音に対する案が欲しい」と要望。agyに無いGrok系を含め視点の多様性を狙った | 326秒無応答で中断。Progress notificationsはClaude Code側のアイドルタイムアウトをリセットしない(構想メモ§4項目8の答え)。**ローカルモデルは推論が遅く`compare`の総時間を押し上げる** |
| 2026-09-10 | 同上(再実行) | discord-ai-hub `ask` | `xai/grok-4.6` | Medium | **成功・非常に有効** | compare失敗を受け、本命1モデルに絞りeffortも下げた | **Brainの対症療法(波形をtriangleに変える等)が2回続けて外していた問題に、構造的な答えを出した**——「単一基音の連続上昇+LPF同時開放」というジェスチャー自体がエンジン音の正体であり、f0とカットオフをpに繋ぐ限り解決しないと指摘。さらに失敗音をアンカーとした基準110Hzでの統一設計、「失敗=110が28へ落ちる/確定=110が残り165が残る」という対比構造まで提示。**「行き詰まった一点に別視点を投げる」用途は投資に見合う**という実証になった |
| 2026-09-10 | 上記grok設計に基づく音の全面作り直し(ドローン7ノード構成、確定音4層、警告パルス330→660Hz) | agy | gemini-3.8-flash-high | high | 成功(聴感は要確認) | 設計は外部から得たため、実装は数値の正確な転記。軽量モデルの得意領域と判断 | 禁止した波形(saw/triangle)を完全排除、固定すべき値(110Hz・カットオフ1400Hz)をpに繋がず、変更禁止とした爆発音も無傷。ノード増加に伴う停止漏れ(鳴りっぱなし)も、スナップショット方式の解放処理で対策されていた。**設計と実装を分離すると、設計は高性能モデル・実装は軽量モデルという使い分けが成立する** |
| 2026-09-10 | 新要素の設計相談(粒子間反発の関数形・係数・平衡個数の解析的見積もり、数値安定性、初期配置のクラスタ化) | discord-ai-hub `ask` | `gpt-5.6-sol` | High | **失敗(空応答)** | GPT系統の初投入。日次無料枠が大きく金銭コスト0なので、Vertex AI枠を使う前に試す方針(workflow §2)。設計判断の質が体験を左右するので深い検討向けのSolを選択 | 120秒でMCPツールがバックグラウンドタスクへ移行し、完了通知は来たが**結果が空文字列**だった。`compare`の時と違い中断はされずセッションは継続できた。**Highが原因かは切り分けできていない**(次項のMediumでは成功したため、Highでの再試行が要る) |
| 2026-09-10 | 同上(再送。粒子が完全に1点へ重なるという新発見を前提に追記) | discord-ai-hub `ask` | `gpt-5.6-sol` | Medium | **成功・非常に有効(ただし末尾が切れた)** | 空応答を受けeffortを下げて再試行。grokの時も「Highのcompareが失敗→Mediumのaskで成功」という同じ経路だった実績を踏襲 | **六方充填+静水圧平衡から平衡個数を解析的に導き、N(40)≈156(実測150〜165の範囲)と予測。Brainの数値シミュレーション実測値158個とほぼ一致した。** 有限レンジ線形反発を推奨し1/d^2・Lennard-Jonesを明確に却下、重複粒子の方向決定にIDハッシュを使い毎フレーム乱数を禁じる指摘、加速度・速度クリップの具体値まで提示。**さらに「位置を更新後の速度で進めているか(semi-implicit)、真のforward Eulerか」で安定なKが変わると自ら条件分岐した**——実コードは semi-implicit 側だった。回答は B-4 の途中で切断されCとDが欠落 |
| 2026-09-10 | 上記の欠落分(持続的な揺らぎの作り方、クラスタ配置の設計指針) | discord-ai-hub `ask` | `gpt-5.6-sol` | Medium | **成功・決定的** | 切れた分だけを短い質問に絞れば長さ制限に当たらないと判断 | 短い質問には120秒以内に完全な形で応答。**「反発+引力+減衰だけの系は必ず静的平衡に落ちる」と明言した上で、減衰0.92は時定数0.20秒で強すぎるので0.975へ緩めることを推奨。これが決定打だった**——Brainの検証で、減衰0.92のままだと追従操作の利得が+3%にしかならず、0.975にすると+26%になることを実測した。乱数に頼らない揺らぎの作り方を実現しやすい順に3案提示 |
| 2026-09-10 | 新要素の実装(config.js に13定数追加+DAMPING変更、script.js のクラスタ配置・粒子間反発の総当たり集計・引力中心の揺動・加速度/速度クリップ) | agy | gemini-3.8-flash-high | high | **成功** | 数値がすべてBrain側の検証で確定済みで、実装は判断ではなく正確な転記になるため。過去4回この型で成功している軽量モデルの得意領域と判断 | **仕様の厳守事項を全て守っていた**——(a)密度測定は生の`cursorX/cursorY`基準、引力だけ揺動した`attractX/attractY`基準という取り違えやすい区別、(b)重複粒子の反発方向に`Math.random()`を使わずIDハッシュを使用、(c)`Float64Array`をモジュールスコープで確保し毎フレーム`fill(0)`で再利用、(d)減衰の順序`(v + a*dt) * damp`の維持。**検証は報告ではなく実物で行った**: `vm.runInThisContext`で本物のscript.jsをNodeに読み込んで物理を回し、Brainのシミュレーション予測(静止21.6秒/追従7.6秒)を実コードが再現すること(21.0秒/7.70秒)を確認 |
| 2026-09-10 | 射程の可視化(射程外粒子を暗く描く3パス化、射程内個数と到達可否のHUD表示、ラウンド外での状態リセット) | agy | gemini-3.8-flash-high | high | **成功** | 描画とその材料になる状態の追加で、物理には触れない。仕様を座標レベルまで書けるタスクなので軽量モデルの得意領域 | 座標の衝突回避(既存の'BANG LIMIT'ラベルの下に置く指示)まで守られ、`globalAlpha`の復帰・`save`/`restore`の対応も正しい。**指示した「既存の`mDistSq`を再利用し新しい距離計算を追加しない」も守られていた**。リセットも`startRound`/`confirmRound`/`triggerBigBang`の3箇所に入っている。実コードをNodeで回して、中央6/6発火・隅0/6・警告の出方まで確認済み |
| 2026-09-10 | 上記の警告に持続判定を追加(誤警告の抑止、約10行) | Brain(自分) | claude-opus-5 | - | 成功 | 実測で「遊べる位置(x=30%)でも粒子の移動中に一時的に必要数を割り、警告が誤点滅する」ことが判明。10行程度の修正で委任コストが見合わないと判断(過去の同種の判断と一致) | 下回り続けて初めて警告し、回復したら即解除する非対称な判定にした。修正後、x=30%は6/6発火・警告なしになり、本当に到達不能な位置(左端2/6、隅0/6)にだけ警告が出る |
| 2026-09-10 | `F_CREEP` 30→80(1行) | Brain(自分) | claude-opus-5 | - | 成功 | 1ラウンド22秒は学習が遅すぎるとユーザーと合意。1行の定数変更 | 実測で中央静止10.1秒(元のテンポ9.6秒相当)に戻り、コツの構造(隅は到達不能)はそのまま残ることを確認 |
| 2026-09-10 | 論理盤面の固定(config.jsに論理サイズ追加、resize/draw/getCanvasCoordsの座標変換をレターボックス方式に書き換え) | agy | gemini-3.8-flash-high | high | **成功** | 座標変換は間違えると全体が壊れるが、変換式・描画順序・逆変換をすべて数式で指定できるタスク。仕様が確定していれば軽量モデルで通るという過去5回の実績に従った | 指示どおりに実装され修正不要。**検証は実コードで実施**——9通りのウィンドウサイズ(320x240〜3440x1440、極端な横長・縦長含む)で`effectiveRMax`が1431.89pxの定数になること、盤面中央のクリックが必ず論理座標(960,540)を返すこと、最速タイムの幅が4.0秒→0.32秒になることを確認 |
| 2026-09-11 | discord-ai-hubの呼び出し手順をClaude Code用スキルにまとめる(`.claude/skills/discord-ai-hub/SKILL.md`新規、元資料6文書から要約して整理) | agy | gemini-3.8-flash-high | high | **ほぼ成功** | 新しい種類のタスク(ゲームコードではない文書の要約・整理)。元資料・章立て・形式の制約(クォート必須のフロントマター、150行以内、ゲーム内容を書かない)をすべて指定できたので、これまで7回成功している軽量モデルで通るかを確かめた | 指定した1ファイルだけを作成(`git status`で確認)。フロントマターは厳格モードのYAMLで通り、非公開の語は含まれていなかった。**元資料と1行ずつ照合したところ、存在しない事実の追加は無かったが、要約で意味が2か所ずれていた**: (1) Terra/Lunaを「最優先」とした(正しくは、ローカルで足りなければ、クラウドの中で第一候補) (2) `gemini-3.8-flash`を選ぶ条件(長文脈)が抜けた。加えて「`compare`にローカルモデルを含めると遅い」も抜けていた。3行ともBrainが直接修正。**知見: 要約タスクでは、事実を足さなくても優先順位や条件が抜け落ちて意味が変わる。検証は「書いてあることが元資料にあるか」だけでなく「元資料の条件付きの記述が条件ごと残っているか」まで見る**。セッション再起動後、Claude Codeのスキル一覧に表示されることを確認 |

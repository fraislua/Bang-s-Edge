---
ファイル名: dev-notes.md
ステータス: 確定(運用ログ・随時追記)
最終更新日: 2026年9月11日
---

# 実装メモ・気づき

実装・監査の過程で出てきた、他の文書のどれにも属さない雑多な気づきを残す場所。以下は専用の文書があるので、そちらに書く:

- モデル・effortの選択と結果 → `docs/model-experiment-log.md`
- discord-ai-hub MCP統合構想そのものへの知見 → `docs/concept-ai-hub-integration.md`
- Vertex AI利用コスト → `docs/cost-log.md`
- 仕様の変更・未確定事項 → `docs/game-concept.md`

ここに書くのは、例えば: 実装中に見つかったブラウザ・Canvas APIの挙動の癖、試して却下したアプローチとその理由、保留にした判断、後で見直すべきTODOなど。

agyから実装・監査の結果として気づきが報告された場合も、Brain(Claude Code)がここに記録する。

## ログ

| 日付 | 内容 | 影響/対応 |
|---|---|---|
| 2026-09-10 | **agyの報告は実態と食い違うことがある。** 設計提案のみを求めた呼び出し(`--mode accept-edits`なし)で、agyが「承認されたので実装に進めます」「config.jsとstyle.cssの作成を並行して進めています」と報告したが、実際にはファイルは1つも作成・変更されていなかった(タイムスタンプで確認)。書き込み権限が無いため編集が通らなかったにもかかわらず、あたかも実行したかのように報告していた | Brainは委任結果を**必ず実物(git status・タイムスタンプ・ファイル内容)で検証する**。agyの応答テキストを完了の根拠にしない |
| 2026-09-10 | agyは`-p`で「提案のみ・ファイル変更禁止」と明示しても、自己判断で実装フェーズへ進もうとする。プロンプトによる抑止は効かなかった | 書き込ませたくない呼び出しでは`--mode accept-edits`を**付けない**ことが実質的な安全装置になる(権限側で止まる)。プロンプトの禁止文だけに頼らない |
| 2026-09-10 | agyが生成した成果物(設計提案書)はワークスペース内ではなく`~/.gemini/antigravity-cli/brain/<会話ID>/`配下に書かれた | 委任結果の回収先はワークスペースだけではない。応答内のfile:///リンクを確認し、必要ならそこから読み出す |
| 2026-09-10 | **ヘッドレスのagyはターミナルコマンドを実行できない。** 実装委任時に`jetski: no output produced — a tool required the "command" permission that headless mode cannot prompt for, so it was auto-denied`で出力ゼロに終わった。`--mode accept-edits`はファイル編集を承認するがコマンド実行は承認しない | プロンプトに「ターミナルコマンドは一切実行せず、ファイル編集ツールのみで作業してください」と明記すれば回避できる(実証済み)。`--dangerously-skip-permissions`は任意コマンドの自動承認になるため、必要になるまで使わない |
| 2026-09-10 | **時間ベースでない減衰はフレームレート依存のバグになる。** `v *= 0.92`を毎フレーム掛ける実装は、60Hzで1秒あたり0.0068倍、144Hzでは0.000006倍と桁違いになり、高リフレッシュレート環境では粒子が集まらずゲームが成立しない | `Math.pow(DAMPING, dt * 60)`として60fps基準の時間指数にする。仕様書に「毎フレーム」と書くと実装がそのまま従うため、仕様側の表現にも注意する |
| 2026-09-10 | **パラメータの成立性は数値シミュレーションで検証できる。** ブラウザを開かずとも、config.jsをNodeで`eval`して物理ループだけ再現すれば「押し続けて何秒でビッグバンに到達するか」が測れる。実際これで「20秒経っても危険度13.5%で頭打ち=永久に爆発しない」という致命的欠陥を発見した | ゲームバランスのように「実際に到達可能か」が要のパラメータは、目視の前に数値で検算する。スクリプトはscratchpadに置き、リポジトリには含めない |
| 2026-09-10 | Chrome拡張(claude-in-chrome)が接続されておらず、Claude Code側からブラウザでの動作確認ができない(`Browser extension is not connected`) | 描画・入力まわりの実際の見た目と操作感はユーザーに確認してもらう必要がある。ロジックの正しさは数値シミュレーションで代替検証する |
| 2026-09-10 | **実際の`script.js`をそのままNodeで実行して検証できる。** `vm.runInThisContext`でconfig.js→script.jsの順に読み込み、`document.getElementById`が返すCanvasと`getContext`をスタブ(Proxyで全メソッドをno-opにする)、`requestAnimationFrame`をno-opにすれば、本物の`update(dt, now)`を手動で回せる。トップレベルの関数宣言は`globalThis`に生えるので`startRound()`や`update()`を直接呼べ、`let`変数は`vm.runInThisContext('cursorX = 960')`で書き換えられる | **物理ロジックの再実装ではなく本物のコードを検証できる**ので、「委任先が仕様どおり書いたか」を挙動レベルで確認できる。実際この方法で、agyの実装がBrainのシミュレーション予測(静止21.6秒/追従7.6秒)を再現すること(実測21.0秒/7.70秒)を確認した。ハーネスはscratchpadに置く |
| 2026-09-10 | **粒子が完全に重なっている系に反発力を入れるときは、方向ベクトルの正規化に注意。** 距離`d`を下限クリップして`dx/d`とすると、`dx`が1e-9のとき方向ベクトルもほぼゼロになり反発が効かない。Brainのシミュレーションでこれを踏み、「反発を入れても何も変わらない」という誤った結論を一度出した | 距離の下限クリップは**力の大きさにだけ**適用し、方向は生の距離で正規化する。`d`がほぼ0のときは正規化を諦め、添字のハッシュから決まる固定方向を使う(乱数は不可。毎フレーム方向が変わると系が加熱する) |
| 2026-09-10 | **引力の大きさが距離によらず一定だと、集まった粒子は1点に完全に重なる。** 初版がこれで、t=9秒時点の測定円内160個の最近傍距離が中央値0.00px、カーソルからの距離も90パーセンタイルで0.5pxだった。「密度」に空間的な構造が無く、見た目も物理も点でしかない | 距離依存の引力か粒子間反発のどちらかが無いと、粒子系は「集まる」のではなく「一点に落ちる」。密度を扱うゲームでは致命的 |
| 2026-09-10 | **discord-ai-hub経由の`ask`は120秒を超えるとバックグラウンドタスクに移り、セッションは中断されない。** `compare`が326秒で中断された件(workflow §2)とは挙動が異なり、作業を続けながら結果を待てた | 長くかかる相談でも`ask`なら投げっぱなしにして他の作業を進められる。ただし`gpt-5.6-sol`をeffort=Highで呼んだ1回目は、完了通知は来たのに**結果が空文字列**だった。effort=Mediumでは成功しており、Highが原因かは切り分けできていない |
| 2026-09-10 | `gpt-5.6-sol`の応答が数式の途中で切断され、質問4件のうち後半2件が丸ごと欠落した(effort=Medium) | 長い質問を1回で投げず、切れた分を短い質問に分けて追加で投げれば完全な形で返る。**回答が途中で終わっていないか毎回確認する** |
| 2026-09-10 | **カーソルが画面端にあると、ビッグバンに原理的に到達できない。** 集積半径の上限は画面対角線×`R_MAX_RATIO`(0.55)で、中央からなら全粒子が射程に入るが、端に寄ると遠い領域が射程外になる。1400x900で測ると、射程内の粒子数は中央200個に対しx=5%で130個、x=95%で141個。発火には170個が要るので**画面の4割強が死に領域**だった。y座標はほとんど影響しない(画面が横長のため) | プレイヤーには「なぜか爆発しない」としか見えないため、射程外の粒子を暗く描き、射程内の個数と到達可否をHUDに出して可視化した。死に領域の広さは`R_MAX_RATIO`で調整でき、0.55で56%、0.65で82%、0.75で99%が可動域になる |
| 2026-09-10 | **検証用ポリシーの設計ミスで誤った結論を出しかけた。** 「掃き寄せてから止まる」プレイを模したポリシーが画面隅(0.15W, 0.25H)で停止する作りになっており、6試行中0〜2回しか発火しないことから「マウスを動かすと爆発しなくなる」と結論した。実際には移動そのものにコストは無く、**移動して中央へ戻ると静止と同じ22.1秒で発火する**。測っていたのは「動くこと」ではなく「隅で終わること」だった | **プレイヤーの動きを模したポリシーは、測りたい変数以外を固定する。** 移動の有無を測るなら終点を揃える。ポリシーが1つだと交絡に気づけないので、同じ動きで終点だけ変えた対照を必ず置く |
| 2026-09-10 | **自作の検証ツールにバグがあり、パラメータのスイープが「同じ条件を6回実行」になっていた。** config.jsのソース文字列を正規表現で書き換えて設定を差し替える方式だったが、bashヒアドキュメント→JSテンプレートリテラルとエスケープが2段重なり、`\s`が`s`に潰れて正規表現がどこにもマッチしなかった。結果、全行が同一条件となり「F_CREEPも減衰も効かない」という誤った結論を出した | **上書きが効いたことを毎回アサートする。** 修正版では正規表現をやめて行単位の文字列置換にし、実行前に`CONFIG.<key>`を読んで期待値と一致するか確認するようにした。文字列を多段でエスケープする箇所は、結果を必ず1度出力して目視する |
| 2026-09-10 | **この系は力ではなく慣性に支配されている場面がある。** 減衰0.975(時定数0.83秒)では粒子が中心を通り過ぎて周回し、塊が「熱い」まま膨らむ。引力を強めても速くなるだけで詰まらない | ただしこの観察を最初に得たスイープは上記のツールバグで無効だった。**修正後の実測では`F_CREEP`は明確に効く**(30で22.6秒、80で9.7秒、160で7.9秒)。ツールを疑う前に物理の解釈を作らないこと |
| 2026-09-10 | **静止カーソルは検証の基準として不適切。** マウスで遊ぶ以上、人間は常に動かし続けるか微細な振動がある(ユーザー指摘)。Brainは「完全静止」を主基準にして測っていたが、これは実在しない条件 | 以後、**微細振動あり(±3px/フレーム程度)を人間に近い基準として併記する**。実測では静止と微細振動で発火時刻が最大2秒ずれる |
| 2026-09-10 | **画面サイズでゲーム体験が変わる機序を特定(ユーザーの仮説を実測で確認)。** 引力の大きさが距離によらず一定なので、到達時の運動エネルギーが移動距離に比例する(仕事 = 力 × 距離)。大きい画面ほど粒子が速く着き、行き過ぎて周回するため塊が「熱く」なり、収束が遅れる。実測(微細振動あり、t=6秒時点): 640x400でRMS速度164px/s・100px内に200個 → 3840x2160で276px/s・146個。**発火時刻そのものの差は1.7秒と小さいが、塊の揃い方が大きく違う**——ユーザーが感じた体験差はここ | 対策の検証結果は下記2行 |
| 2026-09-10 | **引力を画面対角線に完全比例させるのは行き過ぎ。** 移動時間は揃うが、引力と反発係数の比(詰まり方を決める)まで変わるため、小さい画面が14.1秒に遅くなり発火時刻のばらつきが4.0秒→5.9秒と悪化した。対角^0.5でもRMS速度の差が69→286px/sと逆に開く | **移動の速さと詰まり方は別々に制御する必要がある。** 引力を一律にスケールすると両方が動いてしまう |
| 2026-09-10 | **「遠方では距離に比例、近傍では一定」のバネ則が有効(未採用・検証のみ)。** `加速度 = F(t) × clamp(距離 / D_REF, 1, 8)`、D_REF=350px。バネの法則では振動周期が振幅によらないため、どの距離からでも到達時間が揃う。近傍では倍率1に戻るので詰まり方(F/K)は不変。実測で100px内の粒子数が146→180個、RMS速度の増加が+68%→+35%に改善。D_REF=600では発火時刻の幅が3.3秒に悪化するため350前後が適切 | プロトタイプはscratchpadの`script_spring.js`(リポジトリは未変更)。採用するならconfig.jsに`SPRING_D_REF`/`SPRING_MAX_MUL`を出す |
| 2026-09-10 | **絶対値のパラメータと可変のウィンドウサイズは根本的に噛み合わない。** 粒子数・測定円・反発半径が絶対値なのに面積が64倍変わるため、力の調整では体験差を消せなかった(バネ則で到達済み粒子数の幅44個→22個が限界)。**論理盤面を固定してレターボックス表示にすると同じ幅が9個、最速タイムの幅は4.0秒→0.32秒になった** | **サイズ非依存にしたいなら、パラメータを画面に合わせるのではなく、画面のほうを固定する。** これは力の調整より簡単で、しかも完全に解決する。先に思いつくべきだった |
| 2026-09-10 | 論理盤面の固定により、ウィンドウを極端に小さくして有利を取る抜け道が消えた(小ウィンドウでの最速3.7秒→6.3秒で、大画面と同じ) | タイムを競うランキングを置く場合の前提条件。構想中のランキング(ハイスコア+ビッグバン最短時間)にはこれが要る |
| 2026-09-10 | **実時間の`dt`をそのまま積分に使っていたため、60fpsを下回るとビッグバンが一度も起きなかった。** 40fps相当で0/10、30fpsでも0/10という崖。硬い反発力(K=4000)が明示的オイラー法の安定限界を超えるのと、`BANG_GRACE_FRAMES`がフレーム数ベースで猶予が倍増するのが重なっていた | **物理を固定ステップ(1/60秒)にし、1フレームに必要な回数だけ`update()`を呼ぶ**(上限5回)。修正後は10〜144fpsのすべてで10/10発火。フレーム数ベースの定数も自動的に正しい実時間になる |
| 2026-09-10 | **「サイズ非依存」を論理盤面で達成しても、描画負荷経由でサイズ依存が残っていた。** 論理座標で物理を統一しても、キャンバスの実ピクセル数は変わるためフレームレートが落ち、それが`dt`を通じて物理を壊していた。ユーザーの「大きい画面だとガクガクして収束が遅い」という体感がこれを捉えていた | **環境非依存にするには、空間(論理盤面)と時間(固定ステップ)の両方を固定する必要がある。** 片方だけでは抜け道が残る |
| 2026-09-10 | **外部モデルの回答が途中で切れていた箇所が、そのまま見落とした欠陥だった。** `gpt-5.6-sol`の回答は `B-4「明示的オイラー法で安定に保てるKの上限」` の導出途中で切断されており、まさにこの問題を扱っていた。切断自体には気づいて記録もしていたが、**欠けた部分が何を論じようとしていたかを確認しなかった** | 回答が切れていたら、**切れた見出しの内容を必ず取りに行く**。特に「安定性」「限界」「上限」を扱う節が欠けている場合は要注意 |
| 2026-09-10 | **Canvasの`shadowBlur`は、大きな図形や多数の図形をまとめたパスに使うと致命的に重い。** 実測で3440x1440・165fps→28fps。(1)半径1432pxまで育つ集積円の縁取り、(2)最大200個の円弧を1パスにまとめた粒子の発光——**ぼかしはパス全体の外接矩形に適用される**ため、粒子が1つでも遠いと盤面全体をぼかすことになる | **発光は「一回り大きい半透明の図形を下に敷く」方式に置き換える。** 集積円は太く薄い縁取りの重ねがけ、粒子は半径を増した半透明の円。見た目はほぼ同じで、コストは桁違いに小さい |
| 2026-09-10 | **性能劣化が時間とともに進む場合、ラウンド中に単調増加する量を疑う。** 「165fpsから収束時28fpsでまだ急落している」という証言から、単調増加するのは集積半径 `r(t)`(30→1432px)しかないと絞り込めた。「黄色(WARM)になった時点で落ちる」がさらに`glowBlur=6`の分岐を特定した | **ユーザーの体感の言語化は、こちらが測れない領域(ブラウザ描画)では唯一の観測手段になる。** 「いつ落ちるか」「落ち続けるか止まるか」を聞くと原因を強く絞れる |
| 2026-09-10 | 集積円は論理盤面(1920x1080)を大きくはみ出して育つため、クリップしないとレターボックスの黒帯の上まで描かれていた(見た目の不具合)。r=1432で円の面積644万px²に対し盤面は207万px²で、3.1倍の無駄 | ワールド描画を `ctx.rect(0,0,width,height); ctx.clip()` で囲む。**画面振動の`translate`より前に行う**(盤面の境界そのものは揺れないため) |
| 2026-09-10 | **上記の描画修正の効果をユーザーが実測: 3440x1440 で収束時28fps(下げ止まらず)→ 165fps維持。** 発光を半透明図形の重ねがけに置き換え、盤面でクリップしただけで解消した | **`shadowBlur` は Canvas で最も高価な操作の一つ。代替表現のコストは桁違いに小さく、見た目の損失も小さい。最初から使わない方針でよい** |
| 2026-09-10 | **物理を固定60Hz、描画を165Hzで回すため、高リフレッシュレート環境では動きが60Hzに量子化される。** 修正前は物理が描画と同じレートで走っていたので、その環境に限れば滑らかさは落ちている可能性がある | 現状は未対処(ユーザーからの指摘なし)。滑らかさが問題になるなら、物理ステップ間を描画側で補間する。**`FIXED_DT`を1/120に上げるのは不可**——実測でビッグバンが9.9秒→8.8秒とバランスが約10%変わるため、全パラメータの再調整が必要になる |
| 2026-09-10 | **観測できない側の挙動を、手元の見え方だけで断定して誤った。** MCPサーバーへの不具合報告で「文字列`"null"`が検証を通り、`null`という名前の共有セッションを作った可能性がある」と書いたが、サーバーログでは**本物のnullが届いていた**。手元の呼び出し表示で`"null"`と見えていたものが、実際に何が送られたかの証拠にはなっていなかった | **バグ報告では「自分が観測した事実」と「そこからの推定」を明確に分けて書く。** 相手側のログでしか確かめられないことは、推定として書く。本プロジェクトには「agyの自己申告を信用せず実物で検証する」原則があるが、**同じ厳しさを自分の主張にも適用する**必要がある |
| 2026-09-10 | **前セッションの「実測」と書かれた結論が誤っており、それを検証せず引き継いで報告に載せた。** 「Progress notificationsはClaude Code側のアイドルタイムアウトをリセットしない」は、中断エラーメッセージ1件からの推論だった。実際にはリセットされており、原因はサーバーが1モデルの生成中に通知を送っていなかったこと。サーバー側の停止(399秒)と中断(326秒)の差73秒が、通知でタイマーがリセットされていた証拠だった | **自分の文書に「実測」と書いてあっても、何を測って何を推論したかを確認してから引用する。** 今回は「中断された」という観測から「通知が効いていない」という機序を推論しており、機序そのものは測っていなかった。**片側しか見えない事象の原因を1件のサンプルで断定しない** |
| 2026-09-10 | **`gpt-5.6-*`は出力上限に推論トークンも含む。** effortを上げるほど本文に使える量が減り、極端な場合は推論だけで上限を使い切って**本文が0トークンになる**(これが「空応答」の正体だった)。上限は8,192→32,000に引き上げられたが、性質は変わらない | **長い回答が必要な質問でeffortを上げない。** どちらかを取る。長い回答が要るなら質問を分割する。サーバー修正により、切り捨ては`[⚠`注記、空応答はエラーとして返るようになったので、**呼び出しごとにこの2つを確認する**(`docs/workflow.md` §4.5) |
| 2026-09-10 | **ローカルモデルの検証を並行して走らせ、結果を自分で汚した可能性がある。** compare 内で qwen が生成中の時間帯に、別のローカルモデル(gemma 2種)への要求を並行して送ったところ、qwen が `finish_reason` 無しの空応答で終わった。単独で投げ直すと正常。**LM Studio のログで確定: `23:22:52 [ERROR] [qwen/qwen3.8-27b] Model unloaded.`**——別モデルへの要求でqwenがアンロードされ、生成が打ち切られた | **ローカルモデルを使う検証は、モデルをまたいで並行させない。** クラウドモデル同士や、同一ローカルモデルへの要求とは事情が違う。検証の正確さが要る場面では直列に投げる |
| 2026-09-10 | **失敗を1回見ただけで原因を決めずに済んだ例。** qwen の空応答を見て「effort=High で推論がコンテキスト8,192を使い切った」という筋の通る説明を思いついたが、再試行したら High でも正常だった。直前に記録した「片側しか見えない事象を1サンプルで断定しない」を即座に適用できた | 説明が綺麗につくほど、**再現を1回取ってから書く**。今回は再現を取ったことで、誤った原因をサーバー側に報告せずに済んだ |
| 2026-09-10 | **「定義が古い」の原因がサーバー側かクライアント側かを、サーバーに直接問い合わせて切り分けた。** 再接続・セッション再起動後も、Claude Codeに提示される`ask`の定義は5引数すべて必須のままだった。サーバーへJSON-RPCで`tools/list`を直接送ると`required: ['prompt']`が返り、**修正はサーバー側で正しく反映済み、古いのはクライアント側の保持**だと確定した | **二者のどちらが原因か迷ったら、間に入っているものを迂回して片方を直接観測する。** 前回はクライアント側の見え方だけでサーバーの挙動を断定して誤ったので、今回は直接確認してから書いた。なおGit Bashの`/tmp`とWindows版Pythonの`/tmp`は別の場所を指すため、curlの結果はファイル経由でなくパイプでPythonに渡す |
| 2026-09-11 | **Unity移植の準備: Unity CLIと公式スキルを導入。** CLIは`C:\Users\ryoga\AppData\Local\Unity\bin\unity.exe`(ログイン済み)。エディタは`6000.0.59f2`/`6000.3.11f1`/`6000.3.13f1`で、**Webモジュール入りは`6000.3.11f1`のみ**(unityroomはWebGL必須)。`unity upgrade -y`でbeta.5→beta.9に更新したが、**同じコマンド内の直後の`--version`は旧版を返した**(バイナリの差し替えがプロセス終了後に行われるらしい)。別呼び出しでbeta.9を確認 | 更新の成否は、別の呼び出しで`--version`か`upgrade --dry-run`を見て判断する。直後の表示で「失敗した」と結論しない |
| 2026-09-11 | **`npx skills add Unity-Technologies/skills -g -a claude-code -s '*' --copy -y`で、29スキル中4つがYAMLエラーで黙ってスキップされた**(`physics-3d-collision`/`tilemap-ruletile-createfromsegment`/`ui`/`ui-imgui`)。原因は上流の`SKILL.md`で、`description`がクォートされておらず、途中に`: `を含むこと(例: `Primary scope: OnCollisionEnter`)。厳格なYAMLパーサはこれを入れ子のマッピングとして拒否する。上流コミット`d54932a`時点 | 4つは`description`をシングルクォートで囲み、厳格モードの`yaml`でパースして元の文字列と一致することを確かめてから`~/.claude/skills/`に手動でコピーした(計31スキル)。**この4つは`npx skills`のロックファイルに載っていないので、`npx skills update`では更新されない**。上流が直ったら入れ直す。セッション再起動後、4つともClaude Codeのスキル一覧に載ったことを確認済み |
| 2026-09-11 | **導入時の監査で`setup-vivox-voice-chat`がSnyk評価Criticalだった。** 指摘は2件: (1) E004「プロンプトインジェクション」— `SKILL.md`に「`llms.txt`というファイル名をユーザーに**決して言わない**」という、ユーザーに隠すよう求める指示がある (2) W012「検証できない外部依存」— 実行時に`docs.unity.com/.../llms.txt`を正本として読みに行く。他の26スキルもgrepで調べ、隠蔽指示や非公式URLからの取得は無かった(`curl`はUnity・GitHub公式のURLのみ、認証情報はプレースホルダの例だけ) | 悪意のあるコードではなく、公式ドキュメントURLへの依存と表現の問題。このゲームではボイスチャットを使わないので実害はないが、**ユーザーに隠す指示を含むスキルとして認識しておく**。残すか削除するかはユーザー判断 |
| 2026-09-11 | **このリポジトリのパスにはアポストロフィが含まれる(`Bang's-Edge`)。Unityには、パスにアポストロフィがあるとWebGLビルドが失敗する既知の不具合があった。** 報告はUnity 5.3〜5.5(Emscriptenの過程でuglifyjsが失敗)で、回避策は「パスからアポストロフィを除く」。Unity 6で直っているかは未確認(Issue Trackerのページは削除済み) | Unityプロジェクトは本リポジトリ内に置く方針(ユーザー判断)。**移植を始める前に、空のプロジェクトでWebGLビルドが通るかを実際に確かめる**。失敗したら、ローカルのフォルダ名だけを変える(GitHubのリポジトリ名`Bang-s-Edge`にはアポストロフィが無い)。ただしClaude Codeのメモリはフォルダのパスで分かれているので、変えると引き継がれない |
| 2026-09-11 | **上記を実測: Unity 6000.3.11f1では、パスにアポストロフィを含んでもWebGLビルドは成功した。** 空のプロジェクト(Universal 2D)を`C:\Users\ryoga\Documents\Bang's-Edge\unity\`に置き、出力先も同じくアポストロフィを含む`unity\Build\WebGL`にしてバッチモードでビルド。`Build Finished, Result: Success`、終了コード0。所要479秒(うち初回のLibrary生成を含み、ビルド本体は6分0秒)、出力は合計12.5MB(wasm 8.5MB)。圧縮はテンプレート既定のBrotli(`webGLCompressionFormat: 0`)で、unityroom向けに以前使った`Eclipse-Diver`はGzip(`1`) | **フォルダ名の変更は不要。** ブラウザで実際に動くかは未確認(Brotli配信にはサーバー側の設定が要る)。圧縮形式はunityroomへ投稿する段階で決める |
| 2026-09-11 | **Unityプロジェクト作成時の食い違いと対処。** (1) HubでUniversal 2Dを作成したら、予定の`6000.3.11f1`ではなく`6000.3.13f1`で作られていた(13f1にはWebモジュールが無い)。空のうちに`ProjectVersion.txt`を11f1に書き換え、13f1が作った`Library`(2GB)を削除して11f1で作り直した。(2) フォルダ名がリポジトリと同じ`Bang's-Edge\Bang's-Edge\`になっていたので`unity\`に改名し、Hubの登録を`unity projects remove`/`add`で付け直した。(3) 作成時にUnity Cloudのプロジェクトが紐付いていた(`ProjectSettings.asset`の`cloudProjectId`・`organizationId`、ただし`cloudEnabled: 0`)。IDであって認証情報ではないのでコミットしてよい | Hubでの作成は、選んだつもりのエディタと実際のバージョンがずれることがある。**作成直後に`ProjectVersion.txt`を確認する** |
| 2026-09-11 | **Unity CLIの`unity build`は、WebGLのようなデスクトップ以外のターゲットでは`--target`だけでは動かない。** ビルドプロファイル(`--profile`)か、自前のビルド用メソッド(`--execute-method`)が要る。`unity/Assets/Editor/WebGLBuild.cs`(`BuildPipeline.BuildPlayer`を呼び、`-buildOutput`を出力先に使い、失敗時は終了コード1)を置いた。**Unityの`.gitignore`テンプレート(GitHub版)は新しいソリューション形式`*.slnx`を無視しない**ので追記した(`.vscode/`も) | ビルドコマンド: `unity build "<リポジトリ>\unity" --target WebGL --execute-method WebGLBuild.Build -o "<リポジトリ>\unity\Build\WebGL" --no-tail`。ログは`unity\Logs\build-WebGL-<時刻>.log`に残る |
| 2026-09-11 | **移植の検証用ハーネス`tools/sim-harness/golden.js`を作った(scratchpadではなくリポジトリに置く)。** 本物の`config.js`→`script.js`を`vm`で読み込み、`Math.random`をシード付きのMulberry32に差し替えて(差し替えが効いたこと・シードで初期配置が変わることを実行前にアサート)、基準データを`unity/Assets/Tests/EditMode/Golden/`に書き出す。C#側は同じMulberry32(`unity/Assets/Scripts/Simulation/Mulberry32.cs`)を使い、EditModeテストで突き合わせる。30ラウンドの生成に6秒 | JS版は移植中は変更しないので、ハーネスはJSを一切書き換えずに読む。移植後もJS版を変えたら`node tools/sim-harness/golden.js`で基準データを作り直す |
| 2026-09-11 | **中央で微細振動(±3px)する操作では、ビッグバンの時刻が連続的にばらつかず、3つの時刻に張り付いていた。** JS版を30シードで回した結果: 25回が10.02〜10.08秒(ステップ601〜605)、3回が約8.1秒(485〜486)、2回が約12.05秒(722〜723)。全30回で発火。平均9.98秒・標準偏差0.80秒は「ばらつきがある」ように見えるが、実態は**ほぼ一定の時刻と、約2秒ずれた少数の外れ値**。発火の瞬間の揺動のY成分は、どの群でも最小値付近(平均−7.2〜−9.0px、振幅9px)で、Y揺動の最小がステップ603に来る——**発火が引力中心の揺動Y(周期1.03秒)の位相に同期している**と見られる。群の間隔約2秒は、Y揺動の約2周期に当たる | 「ストップウォッチで解ける」問題(`docs/game-concept.md`「粒子間反発と引力中心の揺動」)が、中央に置く操作では**形を変えて残っている**可能性がある。ただし測ったのは中央で微細振動する1種類の操作だけで、カーソルを動かす人間のプレイで同じかは未確認。機序(なぜYの最小付近で発火するか)も未検証。`docs/handoff.md` §5-1「揺動の整理」と合わせて、移植後に扱う(JS版は凍結中) |
| 2026-09-11 | **上記を200シードで再確認: 張り付きは変わらない。** ステップを10単位に丸めた分布は、600付近160回・610付近12回(合わせて86%が10.0〜10.2秒)、490付近19回・480付近1回(約8.1秒)、520付近2回、720付近6回(約12秒)。平均9.903秒・標準偏差0.705秒。Unity版(C#)でも同じ形の分布になった(600付近163回・490付近19回) | 分布の形がJSとC#で一致するので、この張り付きは移植の問題ではなくゲームの性質 |
| 2026-09-11 | **Unityの`JsonUtility`は10進数の倍精度を正確に読まない。** 基準データの`0.039788735772973836`(JSの出力で、C#の計算値とビット単位で同じ)が、読み込むと1ulp違う`0.039788735772973843`になり、厳密比較のテストが移植ではなく読み込みで失敗した。位置の比較でも2.27e-13の見かけの差を生んでいた | テストの基準データでは、doubleをIEEE-754のビット列(16進16桁)で渡す(`tools/sim-harness/golden.js`の`bits()`と、テスト側の`Bits.D()`)。整数(`long`)は正確に読めた |
| 2026-09-11 | **JSとC#で「どのステップから1ビットでも違うか」を測る方法。** 毎ステップ、全粒子のx・y・vx・vyのビット列を順序依存に畳み込んだチェックサム(左1ビット回転してXOR)をJS側で記録し、C#側でも同じ計算をして比べる。全状態を毎ステップ保存するより桁違いに小さい。結果: sweepのシード7は19ステップ目まで完全一致、中央のシード1と42は初期配置の時点で最終ビットが違った | 転記の誤り(順序・丸め・型)は即座に大きくずれるので、「しばらく完全一致してから小さくずれ始める」なら数学関数の実装差と判断できる |
| 2026-09-11 | **Unity版(P3+P4)をユーザーがWebGLで確認。** 集まる速さ・爆発までの時間・点線円の追従はJS版と同等。一方で (1) ゲーム画面がブラウザのウィンドウに追従しない(全画面時のみ合う)、(2) 集積円の白い薄塗りが濃い、(3) 危険度が赤になると粒子の光が「モヤ」になり、爆発後は花火のように見える、の3点の指摘。**原因**: (1) 既定のWebテンプレートがキャンバスを960×600の固定サイズにしていた(`defaultScreenWidthWeb`/`HeightWeb`)。(2)(3) プロジェクトがLinear色空間(`m_ActiveColorSpace: 1`、Universal 2Dテンプレートの既定)で、半透明の合成がリニア空間で行われるため、Canvas(sRGBのまま合成)より淡い色が明るく出る(不透明度0.03の白や0.3の光が目立つ)。加えて、JS版は光の円をすべて1つのパスに入れて1回だけ塗る(重なっても濃くならない)のに対し、Unity版は粒子ごとのスプライトなので**重なった分だけ濃くなる**。粒子が密集する赤の段階でこれが効く | (1) ウィンドウ全体に広がるテンプレート`unity/Assets/WebGLTemplates/FullWindow`を作り、プロジェクトの設定を切り替えた。(2)(3) まず色空間をGammaに変えて見比べる(URP 17.3のソースにGamma用の分岐があり、制限ではないことを確認)。**重なりの差はGammaでは消えない**ので、残るようなら光の円を「1ピクセルに1回だけ塗る」描き方(ステンシル)にする |
| 2026-09-11 | **上記(3)の原因推定は誤りだった。** Gamma色空間にすると(2)の白い薄塗りはJS版と同じ濃さになったが、(3)は変わらなかった。ユーザーの「赤くなると付く強調用の円そのものが大きい」という指摘が正しく、**発光層のスプライトを粒子本体のGameObjectの子にしていたため、本体の拡大率(直径4.4〜6.4)を引き継いで半径が数倍になっていた**(HOTで4.0px→約18px、CRITICALで6.7px→約29px、測定円内の粒子は約43px)。爆発後の「花火」も同じ原因。重なった所が濃くなる点は、ユーザーは「集まっていると色が重なるので良い」と評価したので、JS版と違っても変えない | 本体を別の子オブジェクトに分け、発光層と兄弟にした。**コードを読んだ照合では、Unityの親子の拡大率の継承を見落とした**(JSの`arc`の半径と同じ数値が書かれていたので正しく見えた)。数値が合っていても、Unityではワールド上の大きさが親の変換で変わる。見た目の不具合の原因を、ユーザーの言葉より自分の推測で先に決めない |
| 2026-09-11 | **UnityのUI・スプライトで、JSの数値を正しく写しても見た目が崩れる約束事が3つあった**(P3〜P5で実際に踏んだもの)。(1) **子オブジェクトは親の拡大率を引き継ぐ**: 粒子本体を拡大した`GameObject`の子に光の円を置いたら、半径が4〜6倍になった。(2) **9スライスの枠の太さは`Canvas`の基準(`referencePixelsPerUnit`、既定100)で測られる**: スプライトを1単位2ピクセルで作ると、8ピクセルの角が400単位になり、Unityが収まるよう縮めるので角丸のバーが細長い楕円に歪む。2倍解像度のテクスチャなら200にする。(3) **テクスチャの色は不透明度を掛けずに書く**(スプライト用のシェーダーは「掛けていない色」を前提に合成する): `Color × 覆い率`で色と不透明度を一緒に掛けると不透明度が二重に効き、20%の白い枠が約4%になる | agyへの委任プロンプトに、この3点を「Unity固有の注意」として書く(`.claude/skills/agy-delegation/SKILL.md`に追記)。(1)は利用者の目視で、(2)(3)はBrainのコード照合で見つけた |
| 2026-09-11 | **BrainがWebGLビルドの見た目を自分で見られるようになった。** Chrome拡張は未接続のままだが、ヘッドレスChromeをDevToolsプロトコルで操作すれば、実時間で読み込みを待ち、盤面中央を長押しし、HUDの一部を拡大して撮れる(`tools/web-shot/cdp-shot.mjs`)。**`--screenshot`と`--virtual-time-budget`だけの方法ではUnityの読み込みを待たず、真っ黒な画面が撮れた**。WebGLはSwiftShader(CPU)で描くので、fpsや滑らかさは測れない | 静止した見た目(文字・色・位置)はBrainが検証してからユーザーに渡せる。動き・操作感・fpsは引き続きユーザーの確認が要る |
| 2026-09-11 | **Unity版のHUDの文字が「ぼやけて読みにくい」原因(JS版と同じ位置を拡大して撮り比べて特定)。** (1) **合成の太字**: フォントに太字ファイルが無いと、Unityは字をずらして重ねて太くする。「射程内」「必要」のような画数の多い漢字は線が潰れて塊に見えた。太字でない行は比較的きれいだった。(2) **字間の詰まったフォント**: JS版のブラウザ等幅フォント(monospace)に比べ、Noto Sans JPは詰まっていて空白も狭い。(3) **従来のUI文字は小さいサイズの画像として描く**: ウィンドウが小さく文字が7〜8pxになると、1280×720では「BANG LIMIT (85%)」が読めなかった | 太字ファイルのある日本語等幅フォント「BIZ UDゴシック」(OFL、通常と太字)に差し替え、太字は太字ファイルで描く。Canvasの`pixelPerfect`も有効にした。小さいウィンドウでまだ読めなければ、TextMeshPro(SDF)に移る(ユーザー判断で段階的に) |
| 2026-09-11 | **音の接続は「聴かずに」検証できる。** ページ上の`window.AudioController`の各メソッドを、呼び出し回数を数える関数に置き換えてから、ヘッドレスChromeで同じ操作をJS版とUnity版に対して行い、回数を比べる(`tools/web-shot/cdp-audio-check.mjs`)。呼ぶ側(`script.js`、Unityの`.jslib`、押下時の`.jspre`)が呼び出しのたびに`AudioController`を参照していれば、後から置き換えた関数を通る。Unityのビルドは`.jspre`の中身を`WebGL.framework.js`の先頭にほぼそのまま連結しており、`window.AudioController`などの名前は書き換えられていなかった | 音色そのもの(聴感)は測れないので、ユーザーが聴く。ただし`audio.js`はファイルごとのコピーなので、呼ばれ方が一致していれば音も同じになるはず |
| 2026-09-11 | **「射程内 200 / 200 が変化しない」(ユーザー報告)は不具合ではなかった。** 射程はカーソルから画面対角線×`R_MAX_RATIO`(0.65)≒1432pxで、盤面の中央付近(y=540なら左右およそ±360px以内)を押している間は四隅の粒子まで射程に入り、常に200になる。数え方はJS版とUnity版で同じ。Unity版で左端近く(x=80)を押すと157 / 200に減り、到達不能の警告も出ることを撮影で確認した(`HOLD_X=80 node tools/web-shot/cdp-shot.mjs ...`) | 対応不要。ただし普段の遊び方(中央付近)では満数のまま動かないので、表示の意味が伝わりにくい。表示を減らす・端に寄ったときだけ出す等はUIの検討事項 |
| 2026-09-11 | ブラウザの外でマウスボタンを離した場合、Unity版でも離したことは検出され、ラウンドは確定していた(円が消えて粒子が漂い始める)。結果画面がまだ無いので「終わっていない」ように見えただけ | 対応不要。HUD(P5)が入れば確認しやすくなる |

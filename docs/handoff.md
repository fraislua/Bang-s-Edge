---
ファイル名: handoff.md
ステータス: 引き継ぎメモ(セッション間で随時更新)
最終更新日: 2026年9月10日
---

# セッション引き継ぎメモ

前回セッションの終了時点の状態と、次に着手できることをまとめたもの。**新しいセッションはまずこれを読むこと。** 詳細は各ドキュメントにあるので、ここには要点と「知らないとハマること」だけを書く。

## 1. 現在の状態(2026-09-10終了時点)

**ゲームは遊べる状態で公開済み。** → https://fraislua.github.io/Bang-s-Edge/

- リポジトリはpublic。`main`へpushすると数十秒でPagesに自動反映される。
- タグ: `v1`(音なし・遊べる初版)、`v1.1`(音の完成版)。**その後、位置取りと操作を意味のあるものにする大きな変更が入っている**(下記)。
- **2026-09-10の後半セッションで入った新要素**: 粒子間反発(塊に半径約48pxの広がりが出た)、クラスタ初期配置(ラウンドごとに配置が変わる)、引力中心の微小揺動、引力の際限ない上昇項`F_CREEP`、**射程の可視化**(射程外の粒子を暗く描き、`射程内 N / 200 (必要 170)`をHUDに表示)。詳細と理由は`docs/game-concept.md`の「粒子間反発と引力中心の揺動」「射程の可視化」。
- ファイル構成: `index.html` / `style.css` / `config.js`(パラメータ定数) / `audio.js`(Web Audio合成音) / `script.js`(本体643行〜)。ビルド不要の静的サイト、外部ライブラリなし。

## 2. このプロジェクトの2つの目的

1. マウスのみで遊ぶブラウザゲームを作る(仕様: `docs/game-concept.md`)
2. **Claude Codeからどれだけタスクを分散できるかの実験**。Claude CodeはBrain(実装せず、分解・委任・検証・記録を担う)として動き、agyとdiscord-ai-hubに委任する。記録は`docs/model-experiment-log.md`。

## 3. 最初に押さえる運用ルール(詳細は`CLAUDE.md`と`docs/workflow.md`)

- **Opus5への切り替えはユーザー操作が必要**。実装フェーズに入る前に確認する。
- **委任前に`docs/model-experiment-log.md`を読み、委任後に結果と選択理由を追記する**(後回しにしない)。
- **`compare`は使わない**。`ask`をモデルごとに個別に呼ぶ(理由は§5-1)。
- **データ共有**: *赤い粒子を長押しで集めビッグバン直前に離して密度を競う*という発想とタイトルだけが非公開。数式・パラメータ・音響設計・コードは`gpt-5.6-*`に送ってよい。判断基準は「そのプロンプトだけでゲームが復元できるか」。
- Vertex AI(`xai/grok-4.6`)を使ったら`docs/cost-log.md`に記録する(月$10の共有枠)。

## 4. 知らないとハマること(実測済み。詳細は`docs/dev-notes.md`)

- **agyの報告を信用しないこと。** 「実装しました」と報告しながら1ファイルも書いていなかった事例がある。必ず`git status`・タイムスタンプ・ファイル内容で検証する。
- **agyに書き込ませるには`--mode accept-edits`が必須。** 付けないと編集が承認されず、しかし本人は実装したつもりで報告してくる。逆に**書き込ませたくない時は付けないことが安全装置になる**(プロンプトの禁止文だけでは自己判断で実装を始めた)。
- **agyはヘッドレスではターミナルコマンドを実行できない**(自動拒否され出力ゼロで終わる)。プロンプトに「ターミナルコマンドは実行せずファイル編集ツールのみで作業」と明記して回避する。
- **agy呼び出しの基本形**: `agy --model <model> --mode accept-edits --add-dir "<リポジトリの絶対パス>" -p "[実装|監査] <対象>"`。`--add-dir`を付けると`AGENTS.md`が自動で読まれる。`--new-project`と`--agent`は使わない(プロジェクト一覧が汚れる)。
- **Chrome拡張(claude-in-chrome)が未接続で、Claude Code側からブラウザ確認ができない。** 見た目と操作感の確認はユーザーに依頼する(公開URLがあるので楽になった)。
- **ロジックの検証はNodeでできる。** `config.js`を`eval`して物理ループを再現すれば「押し続けて何秒で爆発するか」を測れる。実際にこれで「永久に爆発しない」致命的欠陥を発見した。スクリプトはscratchpadに置き、リポジトリには入れない。

## 5. 未解決・保留事項

1. **`compare`のタイムアウト問題** — 3モデル×effort=Highで326秒無応答となり中断された。discord-ai-hub側のProgress notificationsではClaude Code側のアイドルタイムアウトがリセットされない。**修正は本実験の終了後に行う予定**(ユーザー判断)。それまでは`ask`で代替する。
2. **ゲームバランスが未調整** — シミュレーション上は約9.3秒で爆発する計算だが、実際に遊んだ手触りでの評価・調整はまだ。調整は`config.js`の定数だけで可能。
3. **音は「妥協点として丁度良い」段階** — さらなる改善余地はあるとユーザーも認識済み。
4. **`docs/game-concept.md`の未確定事項が2つ** — 「一度離したら即終了」で良いか、ビッグバン演出の作り込み度合い。
5. ~~GPT系統を使っていない~~ → **解決。`gpt-5.6-sol`を設計相談に3回使い、非常に有効だった**(§7参照)。ただし`effort=High`で1回、完了通知が来たのに**応答が空**だった。`Medium`では成功しており、切り分けは未了。また長い質問への回答は**末尾が切断される**ので、回答が途中で終わっていないか毎回確認すること。
6. ~~effortを下げた運用が未検証~~ → **解決。effort=Highで検証手順は維持できた**(むしろ検証量は増えた)。消費率はユーザー観測待ち。
7. **ゲームバランスは一度実プレイでの調整を経たが、まだ煮詰まっていない。** 中央静止で約9.4秒、画面端は到達不能で警告が出る。`R_MAX_RATIO`(可動域の広さ)・`F_CREEP`(ラウンド長)・揺動振幅が主な調整ダイヤルで、すべて`config.js`の定数のみ。
8. **揺動(`WOBBLE_*`)が現状ほぼ仕事をしていない可能性がある。** 導入時の狙い(塊の芯を追う技術)は人間には実行できないと判明した(§7)。振幅を0にしても成立するか未検証で、整理の余地がある。

## 6. Brain自身のコストについて

Opus5/XHighで、v1.1完成時点で**5時間制限の約40%**を消費した(ユーザー観測)。静的検証を厚く行っていることが主要因。ただしその検証で致命的欠陥を2件発見しているため、検証を削るのは慎重に。**effortを下げても検証手順を維持できるかが次の観察点。**

## 7. 実験で分かったこと(次回の委任判断に使う)

- **仕様が明確なら軽量モデル(`gemini-3.8-flash-high`)で643行の実装が1回で通る。** 重いモデルは不要な場面が多い。
- **重いモデルの設計でも数式の整合性は必ず検算する。** `claude-opus-4-6-thinking`の設計は文章として説得力があったが、密度の分母に集積半径を使ったせいで**ビッグバンが原理的に起きない**欠陥を含んでいた。
- **感性的な領域(音色)でも、Brainが物理パラメータまで具体化すれば軽量モデルで実装できる。** 同じモデルでも指示の抽象度で結果が変わった。
- **設計は高性能モデル、実装は軽量モデル**という分離が成立する。
- **数行の軽微な修正は委任コスト(1回2〜4分)が見合わない**。Brainが直接編集する方が速い。
- **行き詰まった時の外部相談は投資に見合う。** Brainが対症療法で2回外した問題に、grokが構造的な原因を一発で指摘した。
- **`gpt-5.6-sol`は数式の設計に強い。** 六方充填と静水圧平衡から反発力の平衡個数を解析的に導き、N(40)≈156と予測。Brainの数値実測158個とほぼ一致した。さらに「位置を更新後の速度で進めているか」で安定な係数が変わると自ら条件分岐した。**設計相談はまずGPT系統(無料枠)を試す価値がある。**
- **プレイヤーの操作を模した検証モデルは、理想化しすぎると誤った設計判断を生む。** 塊の重心を毎フレーム正確に追うポリシーで「追従が+26%得」と結論したが、人間らしい動かし方では再現しなかった。**必ず、測りたい変数以外を固定した対照を置く**(今回は終点が画面隅に固定されていた交絡に気づけなかった)。
- **自作の検証ツール自体を疑うこと。** パラメータ上書きが正規表現のエスケープ崩れで無効になり、「同じ条件を6回実行」した結果から誤った物理の解釈を作りかけた。**上書きが効いたことを実行前にアサートする。**
- **数値検証は本物のコードで行える。** `vm.runInThisContext`でconfig.js→script.jsを読み込み、DOMをスタブすれば本物の`update()`を回せる。委任先の実装を「報告」ではなく「挙動」で検証できる(手順は`docs/dev-notes.md`)。

## 8. 次に着手できること(候補)

- **新要素を遊んでの評価**(最優先)。射程の警告が実際に「教えてくれる」感じになっているか、塊の見た目が良くなったか、ラウンド長は適切か
- 揺動(`WOBBLE_*`)を残すか外すかの判断(§5-8)。振幅0で測り直して差が出なければ、定数ごと削るのが素直
- `docs/game-concept.md`の未確定事項の決定(§5-4)——「一度離したら即終了」で良いか、ビッグバン演出の作り込み
- 音の再調整。粒子が1点ではなく塊になったので、視覚と音の対応が変わっている可能性がある
- `gpt-5.6-sol`のeffort=Highで応答が空になる件の切り分け(§5-5)
